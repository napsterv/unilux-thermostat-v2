import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.unilux.thermostat',
  appName: 'unilux-thermostat',
  webDir: 'build'
};

export default config;
